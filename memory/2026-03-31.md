- 诊断 `openclaw-control-ui` / `127.0.0.1:18789` 首页 `HTTP 404` / `Not Found`：已确认此前把 `gateway.controlUi.basePath` 改到 `/openclaw`，后又改回 `/`；但运行中 Gateway 的该变更被判断为需要 restart 且处于 defer 状态，说明配置文件已改回，不代表当前进程已生效。
- 关键结论：`openclaw gateway status` / `RPC probe: ok` 只能证明网关在线，不证明 Control UI 首页已正确挂载；`dist\control-ui` 存在也只能证明构建产物存在，不证明 HTTP 路由已挂载。
- 待继续：基于本地证据继续排查 `dist\index.js` 与实际替代 `web-server-BfTQfWyy.js` 的 bundle，定位 `controlUi|basePath|control-ui|index.html` 的解析与挂载逻辑，并确认触发 `HTTP 404` 的确切 URL/路径。
- 2026-03-31 晚间：与主人完成 Windows 桌面技能第一批实测阶段性结果梳理。已实测 `windows-desktop-control-1.0.3` 截图成功，输出文件示例：`C:\Users\18263/Pictures/OpenClaw\screenshot_20260331_213622.png`。
- 2026-03-31 晚间：`openclaw-pc-control-2.0.0` 初测发现缺依赖 `psutil`；随后按其 `requirements.txt` 补装依赖，复测后 CLI 可用，已通过 `--help`、`screenshot`、`screen-info`、`process list`。截图输出示例：`C:\Users\18263\.openclaw\workspace\screenshot.png`。
- 2026-03-31 晚间：对 `midscene-computer-automation-1.0.3` 完成结构检查。确认该技能目录当前主要为 `SKILL.md` 与 `_meta.json`，未见本地 `scripts/` 执行脚本；`SKILL.md` 指向 `npx @midscene/computer@1` 与 `MIDSCENE_MODEL_*` 环境变量，因此当前更适合作为说明/接入层能力看待，不能与前两者按同一“本地脚本即跑”标准硬比。
- 2026-03-31 晚间：给主人输出了第一批阶段报告，当前阶段性判断为：`windows-desktop-control-1.0.3` 偏轻量直用，`openclaw-pc-control-2.0.0` 偏结构化总控，`midscene-computer-automation-1.0.3` 偏视觉自动化路线的说明/封装层。
- 2026-03-31 23:55 左右：按主人要求截取当前页面，生成截图文件：`C:\Users\18263/Pictures/OpenClaw\screenshot_20260331_235503.png`。
- 2026-03-31 23:54-23:56：主人明确表示“先暂停吧”“你退下吧”。当前相关 Windows 技能实测流程已暂停，后续仅在主人再次唤起时继续；可从第二批测试（如 `pc-control` 的 `window/clipboard/key/mouse`）或整理完整测试总表继续。
